#include <stdlib.h>
#include "visao.h"
#include "escalona.h"

char *visao(transacao *transacoes, int tam) {

    if (1)
        return "SV";   // é equivalente
    else
        return "NV";   // não é equivalente
}